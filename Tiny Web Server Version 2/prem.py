print("hello")
 # req = "Output of " + uri + "<br>"

              # for each in sys.stdin :
              #   req = req + each + "<br>" 
              #   print(each)

              # os.close(parentStdin)